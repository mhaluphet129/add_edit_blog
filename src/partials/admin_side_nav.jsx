const SideNav = ({ style }) => {
  return (
    <div className="blogs-admin-side-nav">
      <button className="blogs-admin-side-nav-btn">
        <i className="fa-regular fa-newspaper"></i> Post
      </button>
    </div>
  );
};

export default SideNav;
